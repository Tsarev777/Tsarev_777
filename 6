#include <iostream>
#include <sstream>

using namespace std;

string addSpaces(string s, int width) {

}

void print(string s, int width) {
    istringstream str(s);
    string word, words;
    str >> words;

    while (!str.eof()) {
        str >> word;
        int len = words.size() + word.size() + 1;
        if (len < width) {
            words += ' ' + word;
        }
        if (len == width) {
            cout << "  "  << words << ' ' << word << "  " << endl;
            words = ' ';
            str >> words;
        }
        if (len > width) {
            cout << addSpaces(words,width) << endl;
            words = word;
        }
    }
    if (words.size() > 0)
        cout << "  " << words << "  ";
}

int main() {
    string str = "asfg aaafah bbbfb gf ht jy ky kyj" ;
    print(str, 20);
    return 0;
}
