#include <iostream> 
#include <cmath> 

using namespace std; 

int main() 
{ 
cout « "insert the number *K* " « endl; 
int k,f=0; 
cin » k; 
for (int i = 0; i < k; i++) { 
f += 9 * pow(10, i); 
} 
k = pow(10, k-1); 
for (k; k <= f; k++) { 
cout « k « endl; 
} 
return 0; 
}
